# This file was generated using the `serve build` command on Ray v2.4.0.

import_path: model_deployment:model

runtime_env:
    working_dir: "https://github.com/Charana123/ray/archive/HEAD.zip"

host: 0.0.0.0

port: 8000

deployments:

- name: Model
